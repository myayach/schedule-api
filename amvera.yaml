meta:
  environment: python
  toolchain: pip
  version: "3.11"

build:
  requirementsPath: requirements.txt

run:
  command: uvicorn main:app --host 0.0.0.0 --port 8000

network:
  port: 8000
  protocol: http
